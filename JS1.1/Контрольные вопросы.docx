1. В чем разница между var и let?
Основное различие между var и let в JavaScript заключается в том, что var имеет область действия функции, а let — область действия блока.

2. Какие типы данных в Java Script вы знаете?
Типы данных в JavaScript включают строку, число, логическое значение, null, undefined, объект, символ и BigInt.

3. В каком типе сохраняется переменная, полученная с помощью команды prompt?
 Переменная, полученная с помощью команды prompt в JavaScript, сохраняется в виде строки.